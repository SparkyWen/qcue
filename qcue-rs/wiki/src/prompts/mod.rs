// QCue S2 — prompt builders. The link-emitting constraints constant + builders, and the verbatim
// 4-phase Dream consolidation prompt (golden-file; the prompt IS the spec — App. A §2.5).
pub mod constraints;
pub mod consolidation;
