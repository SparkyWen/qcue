# Robolectric runs the native-plugin unit tests against a stable Android SDK
# (the compileSdk may be newer than Robolectric's shipped frameworks). SDK 35 =
# Android 15, which exercises the API-33+ branches (notif channels, on-device STT).
sdk=35
